services:
  - type: web
    name: appmwinda
    runtime: python
    buildCommand: "pip install -r requirements.txt && python manage.py collectstatic --noinput"
    startCommand: "python manage.py migrate && python manage.py create_admin --username admin --password Admin@123456 && gunicorn AppMwinda.wsgi:application --bind 0.0.0.0:$PORT"
    envVars:
      - key: DEBUG
        value: False
      - key: SECRET_KEY
        generateValue: true
      - key: PYTHON_VERSION
        value: 3.13.5
      - key: ALLOWED_HOSTS
        value: appmwinda-3pgq.onrender.com,.onrender.com,localhost,127.0.0.1
      - key: DATABASE_URL
        fromDatabase:
          name: appmwinda-db
          property: connectionString
      - key: DATABASE_SSL_REQUIRE
        value: False

databases:
  - name: appmwinda-db
    databaseName: appmwinda
    user: appmwinda_user
